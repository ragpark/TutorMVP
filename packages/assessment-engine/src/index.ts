import type { LearnerMastery, MasteryStatus } from '@tutor/shared';
export function gradeResponse(item:{type:string; correctAnswer?:string|null}, response:string): boolean|null { if(item.type==='SHORT_ANSWER') return null; return (item.correctAnswer ?? '').trim().toLowerCase() === response.trim().toLowerCase(); }
export function updateMastery(existing: LearnerMastery|undefined, score:number, assessedAt=new Date()): LearnerMastery { const prev=existing?.masteryScore ?? 0; const attempt=(existing?.attemptCount ?? 0)+1; const masteryScore = score>=0.85 ? Math.min(1, prev+0.35+score*0.15) : score>=0.6 ? Math.max(prev*0.6+score*0.4,0.45) : Math.max(prev*0.5+score*0.25,0.1); const status:MasteryStatus = score>=0.85&&masteryScore>=0.8?'MASTERED':score>=0.6?'PARTIAL':attempt>1?'NEEDS_REVIEW':'PRACTICING'; return {learnerId:existing?.learnerId ?? '', learningObjectiveId:existing?.learningObjectiveId ?? '', masteryScore:Number(masteryScore.toFixed(2)), confidenceScore:Math.min(1,(existing?.confidenceScore ?? .2)+.2), attemptCount:attempt, lastPracticedAt:existing?.lastPracticedAt, lastAssessedAt:assessedAt, status}; }
